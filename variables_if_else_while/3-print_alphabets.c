#include <stdio.h>
/**
 * main - entry point
 *
 * return : always 0
 */
int main(void)
{
	char l = 'a';
	char u = 'A';

	while (l <= 'z')
	{
		putchar(l);
		l++;
	}

	while (u <= 'Z')
	{
	putchar(u);
u++;
putchar('\n');
return (0);
	
	}
}
