#include "main.h"
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	int s;
	char ch[] = "_putchar";

	for (s = 0; s <= 7; s++)
		_putchar(ch[s]);
	_putchar('\n');
	return (0);
}
