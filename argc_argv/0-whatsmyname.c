#include <stdio.h>
/**
* main - main entry
* @argc: argument counter
* @argv: argument value
* Return: always (0)
*/
int main(int argc, char *argv[])
{
if (argc >= 1)
printf("%s\n", argv[1]);
return (0);
}
